// Kotlin collections are interfaces; the compiler can't prove immutability.
kotlin.collections.*

// kotlin.time.Instant is a value class but the compiler can't prove stability cross-module.
kotlin.time.Instant

// Core model types live in a separate module from the Compose UI code.
dev.bongballe.parkbuddy.model.*
